dynamodb:
  endpoint : http://dynamodb.us-east-1.amazonaws.com
  region: us-east-1
  keyStoreTable: public_key_table

http:
    port: 2222
    adminPort: 2223

logging:

  # The default level of all loggers. Can be OFF, ERROR, WARN, INFO, DEBUG, TRACE, or ALL.
  level: INFO
  loggers:
    com.hello.suripu.sync : DEBUG